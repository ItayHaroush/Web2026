<?php

return [
    /*
    |--------------------------------------------------------------------------
    | דגל גלובלי להפעלת תשלום באשראי (B2C)
    |--------------------------------------------------------------------------
    | כאשר false - אף מסעדה לא תוכל לקבל אשראי, גם אם הגדירה מסוף
    | הפעל רק אחרי שחיבור HYP מוכן לייצור
    */
    'credit_card_enabled' => env('CREDIT_CARD_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | דגל לחיוב מנויי מסעדות (B2B) דרך הקרון
    |--------------------------------------------------------------------------
    | כאשר false - הפקודה billing:charge-subscriptions לא תרוץ (גם אם מוגדר schedule)
    | ברירת מחדל true לשמירה על תאימות לאחור – כבה בסביבות שבהן לא רוצים חיוב מנויים.
    */
    'subscription_billing_enabled' => env('SUBSCRIPTION_BILLING_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | HYP Platform Account (B2B) - חשבון Master לחיוב מנויי מסעדות
    |--------------------------------------------------------------------------
    | Masof + PassP = credentials של בעל הפלטפורמה (אתה)
    | משמש לחיוב מסעדות על מנוי חודשי/שנתי + חיובים חוזרים (soft protocol)
    */
    'hyp' => [
        'base_url'    => env('HYP_BASE_URL', 'https://pay.hyp.co.il/cgi-bin/yaadpay/yaadpay3ds.pl'),
        'masof'       => env('HYP_MASOF', ''),
        'passp'       => env('HYP_PASSP', ''),
        'api_key'     => env('HYP_API_KEY', ''),
        'coin'        => env('HYP_COIN', '1'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Order Payment (B2C) - תשלומי לקוחות על הזמנות
    |--------------------------------------------------------------------------
    | המסוף = hyp_terminal_id של כל מסעדה (לא של הפלטפורמה)
    */
    'order_payment' => [
        'session_timeout_minutes' => env('HYP_SESSION_TIMEOUT', 15),
    ],
];
